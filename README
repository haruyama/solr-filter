cd lucene-gosen-setup
cp .../lucene-gosen-4.0.0-ipadic.jar .
java -jar /usr/share/java/ivy-2.2.0.jar -publish local -publishpattern "lucene-gosen-[revision]-ipadic.[ext]" -revision 4.0.0 -status release -overwrite -ivy ivy.xml
